# API Configuration
REACT_APP_API_URL=http://localhost:8080/api

# Environment
NODE_ENV=development

# Optional: Custom port (default is 3000)
# PORT=3000

# Optional: Browser auto-open (default is true)
# BROWSER=true

# Optional: Error overlay (default is true in development)
# FAST_REFRESH=true